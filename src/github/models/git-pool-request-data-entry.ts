/*
 * GitPoolRequestDataEntry
 * Represents a single Pool request data entry for a git ressource.
 */
export class GitPoolRequestDataEntry {
    public key!: string;
    public value!: string;
}